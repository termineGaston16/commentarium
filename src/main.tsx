import './style.css'
import ReactDOM from 'react-dom/client'
import App from './App'  // Asegúrate de que el componente App esté definido y correctamente exportado

ReactDOM.createRoot(document.getElementById('app') as HTMLElement).render(
    <App />
)
